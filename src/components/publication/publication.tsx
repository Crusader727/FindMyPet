import React, {Component, ReactNode} from "react";
import {publication} from "./publication.scss";

export class Publication extends Component {
    public ender(): ReactNode {
        return (
            <div className={publication}>xuy</div>
        );
    }
}
